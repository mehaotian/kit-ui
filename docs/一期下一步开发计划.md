# kit-ui 一期下一步开发计划

> 更新日期：2026-06-25  
> 前置条件：迭代 1（表单基础闭环）已验收完成，见 `docs/一期升级验收清单.md` §G、`docs/k-form三端冒烟验收清单.md`

---

## 1. 当前进度快照

### 1.1 迭代 1 已完成（表单基础闭环）

| 组件 | 状态 | 演示页 |
| --- | --- | --- |
| `k-input` | ✅ 已完成 | `pages/input/input.uvue` |
| `k-textarea` | ✅ 已完成 | `pages/textarea/textarea.uvue` |
| `k-radio` / `k-radio-group` | ✅ 已完成 | `pages/radio/radio.uvue` |
| `k-checkbox` / `k-checkbox-group` | ✅ 已完成 | `pages/checkbox/checkbox.uvue` |
| `k-switch` | ✅ 已完成 | `pages/switch/switch.uvue` |
| `k-form` | ✅ 已完成 | `pages/form/form.uvue` |
| `k-form-item` | ✅ 已完成 | 同上 + `pages/form-validate/form-validate.uvue` |

**专项沉淀（已完成）**

- 三端冒烟验收：`docs/k-form三端冒烟验收清单.md`
- 规则口径：`docs/k-form校验规则参考.md`
- 发布说明：`docs/k-form发布说明.md`
- 开发交接：`docs/k-form开发进度交接.md`

### 1.2 一期整体完成度（约）

- **已完成**：约 27 个组件（含子组件与 `k-config`）
- **P0 核心缺口**：约 15 个（列表、反馈、导航、轮播、刷新加载、选择器等）
- **迭代 1 DoD**：组件 API、三端冒烟、样式红线 — 均已满足

---

## 2. 迭代 1 收口任务（建议 1~2 天，与迭代 2 并行）

在进入新组件开发前，建议先关闭以下治理项，避免状态漂移：

| 序号 | 任务 | 说明 | 优先级 |
| --- | --- | --- | --- |
| G-1 | 更新 `docs/组件现状盘点与优先级规划.md` | 表单组件状态、批次 A 完成标记 | P0 |
| G-2 | 首页状态与真实实现对齐 | `pages/index/index.uvue` 中表单类已 `completed`；确认展示/反馈/导航类 `planning` 项准确 | P0 |
| G-3 | 清理历史临时文件 | 如 `pages/timeline/timeline copy.uvue`、`pages/collapse/collapse copy.uvue` | P1 |
| G-4 | 组件 README 模板统一 | props / emits / slots / 主题变量四段式 | P1 |
| G-5 | `pages.json` 与 demo 目录对齐 | 首页已引用但无页面的项（list/toast/modal 等）待组件开发时一并注册 | P2 |

### k-form Phase 4 可选项（不阻塞迭代 2）

| 序号 | 任务 | 说明 | 建议 |
| --- | --- | --- | --- |
| F-1 | `scrollToError` | APP 端 `scroll-view` 方案 | 按需评估，非 P0 |
| F-2 | 性能基线实测 | `docs/k-form性能基线记录.md` 补实测数据 | 迭代 2 末期或发版前 |
| F-3 | 计划文档状态终态 | `docs/k-form表单校验开发计划.md` 标记 Phase 1~3 完成 | 顺手同步 |

---

## 3. 迭代 2：展示与反馈闭环（主战场，建议 1~2 周）

### 3.1 迭代目标

解决业务页面「列表承载 + 交互反馈」主链路，使表单提交后的结果展示、确认操作、轻提示具备最小可用能力。

### 3.2 组件任务（按推荐顺序）

#### 第一梯队（强依赖，先做）

| 顺序 | 组件 | 预估 | 关键能力 | 依赖 |
| --- | --- | --- | --- | --- |
| 1 | `k-popup` | 2~3 天 | 弹出层容器、遮罩、position（bottom/center/top）、关闭策略 | 无（后续 modal/toast 的基础） | ✅ v1 已交付，待三端冒烟 |
| 2 | `k-toast` | 1~2 天 | 轻提示、命令式 API（`showToast`）、自动消失 | 可基于 popup 或独立实现 |
| 3 | `k-modal` | 2~3 天 | 确认/取消、标题/内容 slot、遮罩点击关闭 | 依赖 popup 或共用 overlay 层 |

#### 第二梯队（列表承载）

| 顺序 | 组件 | 预估 | 关键能力 | 依赖 |
| --- | --- | --- | --- | --- |
| 4 | `k-list` | 2~3 天 | 列表容器、空态、loading 态、边界项 | 可与 `k-cell` 复用样式思路 |
| 5 | `k-list-item` | 1~2 天 | 列表项、左/右 slot、点击态、长内容省略 | 依赖 k-list |

> **说明**：现有 `k-cell` / `k-cell-group` 已覆盖部分列表场景；`k-list` 应定位为更通用的数据列表容器，避免与 cell 能力完全重复，可在设计阶段明确差异（如虚拟滚动预留、批量操作 slot 等）。

#### 并行增强（可穿插）

| 组件 | 预估 | 说明 |
| --- | --- | --- |
| `k-swiper` | 2~3 天 | 轮播图，首页/ Banner 场景 |
| `k-swiper-item` | 0.5~1 天 | 轮播项，与 swiper 同批交付 |

### 3.3 每个组件默认交付物

沿用 `docs/一期组件迭代看板.md` Issue 模板：

- [ ] 组件 API（props / emits / slots）
- [ ] demo 页面 + `pages.json` 注册
- [ ] README 与实现一致
- [ ] APP / WEB / MP 冒烟验证
- [ ] 满足 kit-ui 一期 rules 红线（无 grid/gap/@media）

### 3.4 迭代 2 DoD（完成标准）

- [ ] 列表场景可承载真实数据项（空态、长内容、边界项）
- [ ] 弹层与提示具备最小可用交互（开关、关闭、禁用态）
- [ ] 表单提交失败/成功可通过 toast 或 modal 演示完整反馈链路
- [ ] 示例文案与组件行为一致
- [ ] 首页对应入口状态更新为 `completed`

### 3.5 技术预研要点（开发前 0.5 天）

| 主题 | 关注点 |
| --- | --- |
| APP 动画 | popup/modal 开合动效走 `kit-ui-app-animation` 技能，禁止响应式 `:style` 驱动 |
| 命令式 API | toast 需评估 UTS 下全局挂载方案（provide/inject vs 单例组件） |
| 层级管理 | popup/modal/toast 的 z-index 与遮罩叠加策略 |
| 与 k-form 联动 | modal 内嵌表单、提交后 toast 反馈 — demo 预留组合示例 |

---

## 4. 迭代 3：导航与滚动能力（迭代 2 完成后，1~2 周）

### 4.1 组件任务

| 组件 | 说明 |
| --- | --- |
| `k-tabs` / `k-tab-pane` | 标签页切换 |
| `k-navbar` | 顶部导航栏 |
| `k-tabbar` / `k-tabbar-item` | 底部标签栏 |
| `k-pull-refresh` | 下拉刷新 |
| `k-load-more` | 上拉加载更多 |

### 4.2 迭代 3 DoD

- [ ] 导航组件可在 demo 中串联成完整页面流
- [ ] 下拉刷新与上拉加载具备可演示的状态切换
- [ ] 文档、示例、组件状态清单三者同步

---

## 5. 表单生态补全（迭代 2~3 间隙，按需插入）

表单核心已闭环，以下组件按业务需求优先级插入，**不建议与迭代 2 主任务并行抢产能**：

| 组件 | 优先级 | 说明 |
| --- | --- | --- |
| `k-picker` | P0（清单） | 滚动选择器，表单常见缺口；复杂度高于 switch/radio |
| `k-search` | P1 | 搜索框，可基于 k-input 扩展 |
| `k-number-box` | P1 | 数字步进 |
| `k-password` | P1 | 密码显示/隐藏 |

---

## 6. 建议 Issue 拆分（迭代 2 可直接创建）

```base
# Issue 1
标题：feat(组件): 新增 k-popup 弹出层（一期迭代 2）
验收：API + demo + README + 三端冒烟 + 遮罩/position/关闭

# Issue 2
标题：feat(组件): 新增 k-toast 轻提示（一期迭代 2）
验收：命令式调用 + 自动消失 + demo + 三端冒烟

# Issue 3
标题：feat(组件): 新增 k-modal 模态框（一期迭代 2）
验收：确认/取消 + slot + 与 popup 层级一致 + demo

# Issue 4
标题：feat(组件): 新增 k-list / k-list-item（一期迭代 2）
验收：空态/loading/长内容 + 与 k-cell 差异说明 + demo

# Issue 5（可选并行）
标题：feat(组件): 新增 k-swiper / k-swiper-item（一期迭代 2 增强）
验收：基础轮播 + 指示器 + 三端冒烟
```

---

## 7. 时间线建议（2026-06-25 更新）

```text
第 1 周（进行中）
├── ✅ k-overlay + k-popup v1 代码与 demo
├── Day 1：三端冒烟 + k-toast 开发
├── Day 2~3：k-modal（基于 k-popup）
└── Day 4~5：组合 demo + P1 完善（beforeClose 等）

第 2 周
├── Day 1~3：k-list + k-list-item
├── Day 4：k-swiper（可选）或迭代 2 验收
└── Day 5：看板/清单状态终态更新

第 3~4 周：迭代 3（导航 + 刷新加载）
```

完整剩余路径见：`docs/k-popup开发进度与后续计划.md`

---

## 8. 风险与决策点

| 风险 | 应对 |
| --- | --- |
| popup/modal 在 APP 端动画不生效 | 提前读 `kit-ui-app-animation`，首周做 APP 验证 |
| k-list 与 k-cell 能力重叠 | 开发前写 1 页差异说明，避免重复建设 |
| 命令式 toast 在 UTS 下难实现 | 备选方案：声明式 `<k-toast>` + ref 调用 |
| 产能分散 | 迭代 2 主任务不超过 5 个组件；picker 等延后到迭代 3 间隙 |

---

## 9. 相关文档索引

| 文档 | 用途 |
| --- | --- |
| `docs/一期组件迭代看板.md` | 迭代任务与 DoD |
| `docs/组件开发清单.md` | 全量组件状态 |
| `docs/组件现状盘点与优先级规划.md` | 批次优先级（待更新） |
| `docs/一期升级验收清单.md` | 一期结项判定 |
| `.cursor/skills/kit-ui-add-component` | 新建组件流程 |
| `.cursor/skills/kit-ui-uts-app-compat` | UTS/APP 编译约束 |
