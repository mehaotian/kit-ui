---
description: "kit-ui 一期核心硬约束（统一口径）"
alwaysApply: true
---

# kit-ui 核心硬约束（一期）

## 目标

- 只保留必须遵守的红线，不在 rules 中堆叠模板。
- 详细流程统一交给 `.cursor/skills/`。
- 冲突裁决以 `docs/规则冲突治理清单.md` 为准。

## 平台与语法红线

1. 页面与组件使用 `.uvue`，脚本使用 `<script setup lang="uts">`。
2. 条件编译统一使用 `WEB / APP / MP-*`，不新增 `H5` 写法。
3. UTS 比较使用 `==` / `!=`，空值判断必须显式。
4. 新增页面必须同步 `pages.json`。

## 样式红线

1. 禁止使用 `grid`、`gap`、`@media`。
2. 字体相关样式只允许写在 `text` 或 `button`。
3. 主题变量统一 `k-` 命名（如 `$k-color-primary`、`--k-color-primary`）。
4. 禁止新增全局污染样式。

## 流程分工（rules -> skills）

- 新建组件：`kit-ui-add-component`
- 文档同步：`kit-ui-doc-sync`
- 跨端检查：`kit-ui-cross-platform-check`
- 视觉精修：`kit-ui-visual-polish-check`
- 发布检查：`kit-ui-release-checklist`
- MCP 使用：`kit-ui-mcp-usage-guide`
