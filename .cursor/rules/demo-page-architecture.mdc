---
alwaysApply: true
description: "kit-ui 演示页硬约束：目录、结构、跨端与样式红线"
---

# kit-ui 演示页硬约束

## 目录与命名

- 页面目录：`pages/[name]/`
- 主文件：`pages/[name]/[name].uvue`
- 新增页面必须同步 `pages.json`

## 页面结构约束

1. 演示页至少包含：基础用法、状态用法、边界用法。
2. 页面需要滚动时，在 APP 场景使用条件编译包裹 `scroll-view`。
3. 演示文案与实际 props/events 行为保持一致。
4. 复杂交互示例必须有中文注释。

## 样式红线

1. 演示页使用 `flex` 布局，不使用 `grid` 与 `gap`。
2. 禁止 `@media`，通过 UTS 条件逻辑做响应式适配。
3. 文本类样式只写在 `text/button`。

## 推荐技能

- 演示页脚手架：`kit-ui-add-component`
- 文档与示例同步：`kit-ui-doc-sync`
- 合规自检：`kit-ui-cross-platform-check`
