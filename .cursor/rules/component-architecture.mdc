---
alwaysApply: true
description: "kit-ui 组件硬约束：目录、命名、组件结构"
---

# kit-ui 组件架构硬约束

## 目录与命名

- 组件目录：`uni_modules/kit-ui/components/k-[name]/`
- 主文件：`k-[name].uvue`
- 文档：`README.md`
- 复杂组件可补充：`types.uts`、`utils.uts`、`config.uts`

## 组件实现约束

1. 组件脚本使用 `<script setup lang="uts">`。
2. 对外能力至少明确三类：props、emits、slots（按组件实际裁剪）。
3. 主题注入键统一使用 `kit-theme`。
4. 事件处理函数与复杂逻辑应添加函数级注释。
5. 禁止在组件中引入与 uni-app x 不兼容的插件模式。

## 样式与状态约束

1. 样式类名前缀统一 `k-[name]`。
2. 状态类统一包含：`--disabled`、`--loading`（按需扩展）。
3. 优先使用 `--k-*` 变量，禁止新增旧变量体系。
4. 文本样式只能写在 `text/button`。

## 推荐技能

- 创建组件流程：`kit-ui-add-component`
- 组件变更后文档同步：`kit-ui-doc-sync`
- 合规检查：`kit-ui-cross-platform-check`
- APP 过渡动画：`kit-ui-app-animation`（状态动效禁止 APP 端用响应式 `:style` 驱动）
