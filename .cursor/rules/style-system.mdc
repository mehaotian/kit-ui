---
alwaysApply: true
description: "kit-ui 样式硬约束：主题变量、跨端限制、视觉一致性"
---

# kit-ui 样式系统硬约束

## 主题变量

1. SCSS 与 CSS 变量统一采用 `k-` 前缀体系。
2. 主色统一 `#6366f1`，禁止新增 `#007aff` 体系。
3. 组件样式优先使用 `--k-*` 变量，不鼓励硬编码色值。

## 跨端样式红线

1. 禁止 `display: grid` 与 `gap`。
2. 禁止 `@media`，改为 UTS 逻辑处理。
3. 避免复杂伪类与平台不兼容 CSS 特性。
4. 文本相关样式仅用于 `text/button`。

## 组件样式约束

1. 组件类名遵循 `k-[component]` 与 `k-[component]--[state]`。
2. 需要交互反馈时，优先 `hover-class` / 状态类控制。
3. 暗色模式通过变量切换实现，避免写死暗色分支样式。
4. 不新增全局污染选择器。

## 推荐技能

- 视觉一致性检查：`kit-ui-visual-polish-check`
- 跨端合规检查：`kit-ui-cross-platform-check`
